name: Android App APK Build

on:
    push:
        branches:
            - dev
jobs:
    build:
        runs-on: ubuntu-latest
        defaults:
            run:
                working-directory: purple
        steps:
            - name: Setup repo
              uses: actions/checkout@v4

            - name: Setup node
              uses: actions/setup-node@v3
              with:
                  node-version: 20.x
                  #   cache: "yarn"

            - name: Set up JDK 17
              uses: actions/setup-java@v3
              with:
                  java-version: "17"
                  distribution: "temurin"

            - name: Setup Android SDK
              uses: android-actions/setup-android@v3

            - name: Setup Expo
              uses: expo/expo-github-action@v8
              with:
                  expo-version: latest
                  eas-version: latest
                  token: ${{ secrets.EXPO_TOKEN }}

            - name: 📦 Install dependencies
              run: yarn install --frozen-lockfile

            - name: Create .env file for build
              run: |
                  echo "EXPO_PUBLIC_API_URL=${{ secrets.API_URL }}" > .env
                  echo "EXPO_PUBLIC_API_KEY=${{secrets.API_KEY}}" >> .env

            - name: 🚀 Build app
              run: eas build --non-interactive -p android --profile development --local --output ${{ github.workspace }}/purple.apk

            - name: Upload Build Artifact
              uses: actions/upload-artifact@v4
              with:
                  name: app-release
                  path: ${{ github.workspace }}/purple.apk
