export const CLEAR_ORDER = [
    'transactions',
    'recurring_transactions',
    'accounts',
    'users',
    'settings',
    'transaction_types',
] as const;

export const INSERT_ORDER = [
    'users',
    'settings',
    'transaction_types',
    'accounts',
    'recurring_transactions',
    'transactions',
] as const;
